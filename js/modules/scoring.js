// BeatMaster - Scoring (Platzhalter - wird in Phase 4 implementiert)
const Scoring = {
  validateAnswer(userAnswer, correct, type) {
    console.log('Scoring (Platzhalter)');
    return false;
  },
  calculatePoints(context) {
    return 0;
  }
};
window.Scoring = Scoring;
