// BeatMaster - Game Engine (Platzhalter - wird in Phase 4 implementiert)
const GameEngine = {
  createGame(config) {
    console.log('Game Engine (Platzhalter)');
    return {};
  },
  startGame() { }
};
window.GameEngine = GameEngine;
