// BeatMaster - Timer (Platzhalter - wird in Phase 4 implementiert)
class Timer {
  start(duration, onTick, onComplete) {
    console.log('Timer (Platzhalter)');
  }
  stop() { }
}
window.Timer = Timer;
